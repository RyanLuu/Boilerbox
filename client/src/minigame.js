class Minigame {
  constructor(name, scene) {
    this.name = name;
    this.scene = [] || scene;
    this.finished = false;
  }
  
  update(input) { } // update scene elements according to user input
  
  end() { 
 
  }

}